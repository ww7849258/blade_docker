# daemon off;
# pid /tmp/nginx.pid;
# worker_processes 2;
# http{
  upstream puma {
    server blade_web1:80 weight=3;
    server blade_web2:80;
  }

  server {
    listen 80;
    server_name loaclhost;
    location / {
    proxy_pass http://puma;
    }
  }
# }